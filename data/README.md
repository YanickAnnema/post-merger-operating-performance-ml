# Data

This folder is intentionally empty in git.

Licensed LSEG/Worldscope data and all row-level derived thesis datasets are not redistributed. For demos, generate synthetic data with:

```powershell
python src/synthetic/make_synthetic_ml_ready.py
```

The generated file is ignored by `.gitignore`.

